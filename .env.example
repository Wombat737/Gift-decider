# Copy to .env.local and fill in. The app runs in demo mode if these are empty.

# --- Supabase (required for real auth + data) ---
# Project Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Use the publishable (anon) key — never put the secret/service-role key in the app
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY

# Optional: local Supabase CLI (npx supabase start)
# EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...   # from `supabase status`

# Public web origin used when composing share links (EAS web or Expo web)
EXPO_PUBLIC_APP_URL=https://localhost:8081

# --- Auth placeholders (production OAuth is NOT wired in this scaffold) ---
# Flip these to "true" only after you finish Apple / Google + Supabase provider setup
EXPO_PUBLIC_APPLE_AUTH_ENABLED=false
EXPO_PUBLIC_GOOGLE_AUTH_ENABLED=false

# Google Cloud OAuth client IDs (placeholders until you create them)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=

# Deep-link scheme is set in app.json (`giftdecider`). Magic-link redirect:
#   giftdecider://auth/callback
# Add that URL under Supabase → Authentication → URL Configuration → Redirect URLs.
# Also add your Expo web origin, e.g. http://localhost:8081
